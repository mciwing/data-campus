[project]
name = "data-campus"
version = "0.2.0"
description = "Course Material for Data and Prompting"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-glightbox>=0.5.2",
    "mkdocs-material>=9.6.20",
]

[dependency-groups]
dev = [
    "zensical>=0.0.53",
]
